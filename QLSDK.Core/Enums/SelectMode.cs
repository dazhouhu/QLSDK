namespace QLSDK.Core
{
    public enum SelectMode
    {
        AUTO = 0		/**< Indicates SDK will select the number of remote video streams automatically. */
	    ,MANUAL			/**< Indicates SDK let user application select the number of receiving remote video streams. */
    }
}
