namespace QLSDK.Core
{
    public enum DTMFKey
    {
         UNKNOWN = 0
        ,ZERO					/**< Number zero. */
        ,ONE						/**< Number one. */
        ,TWO						/**< Number two. */
        ,THREE					/**< Number three. */
        ,FOUR					/**< Number four. */
        ,FIVE					/**< Number five. */
        ,SIX						/**< Number six. */
        ,SEVEN					/**< Number seven. */
        ,EIGHT					/**< Number eight. */
        ,NINE					/**< Number nine. */
        ,STAR					/**< Asterisk key. */
        ,POUND					/**< Pound key. */
        ,A					    /**< Character A . */
        ,B					    /**< Character B . */
        ,C					    /**< Character C . */
        ,D					    /**< Character D . */
        ,MAX
    }
}
