namespace QLSDK.Core
{
    public enum FECCKey
    {
        UNKNOWN = 0
        ,UP					/**< Up control. */
        ,DOWN				/**< Down control. */
        ,LEFT				/**< Left control. */
        ,RIGHT				/**< Right control. */
        ,ZOOM_IN				/**< Zoom in control. */
        ,ZOOM_OUT			/**< Zoom out control. */
        ,MAX
    }
}
