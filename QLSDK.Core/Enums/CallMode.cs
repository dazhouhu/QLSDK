namespace QLSDK.Core
{
    /// <summary>
    /// 呼叫模式
    /// </summary>
    public enum CallMode
    {
        VIDEO = 0,        /**< Initiates call mode is audio-video call. */
        AUDIO             /**< Initiates call mode is audio call. */
    }
}
