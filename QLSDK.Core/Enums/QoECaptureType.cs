namespace QLSDK.Core
{
    public enum QoECaptureType
    {
        RAW = 0       /**< QoE scale data of captured raw. */
        ,SCALE0      /**< QoE scale 0 data of captured raw. */
        ,SCALE1      /**< QoE scale 1 data of captured raw. */
        ,SCALE2		/**< QoE scale 2 data of captured raw. */
    }
}
