namespace QLSDK.Core
{
    public enum RecordWaveFormat
    {
        RAW = 0x0001          /**< The audio stream format is raw. */
        ,PCMU = 0x0006         /**< The audio stream format is PCMU. */
        ,PCMA = 0x0007         /**< The audio stream format is PCMA. */
    }
}
