namespace QLSDK.Core
{
    public enum RecordingPlaybackStatus
    {
        NOT_STARTED = 0  	/**< Recording/playback is not started. */
        ,STARTED           /**< Recording/playback has been started. */
        ,PAUSED            /**< Recording/playback has been paused. */
    }
}
